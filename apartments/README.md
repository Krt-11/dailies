# Polaris Apartment Shortlist

The map no longer uses hand-entered approximate coordinates. Each apartment pin is generated from the exact street address in `app.js` using a geocoding service, then displayed on Leaflet/OpenStreetMap.

The apartment cards also show the exact street address and the map's matched address in each popup.

Files:
- index.html
- style.css
- app.js
